# Codex Hooks

This directory ports hook scripts from the original project.

- `session-start.sh`
- `simplify-ignore.sh`
- `simplify-ignore-test.sh`

Hook wiring is environment-specific. Keep these scripts as reusable building blocks and wire them in your Codex config according to your runtime's hook configuration format.
